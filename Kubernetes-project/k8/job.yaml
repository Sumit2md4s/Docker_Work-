apiVersion: batch/v1
kind: Job
metadata:
  name: pythonapp-job

spec:
  template:
    spec:
      containers:
        - name: job-container
          image: busybox:latest
          command:
            - sh
            - -c
            - echo "Kubernetes Job completed successfully"

      restartPolicy: Never

  backoffLimit: 3
